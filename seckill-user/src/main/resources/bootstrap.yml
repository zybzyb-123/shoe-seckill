server:
  port: 3377

spring:
  application:
    name: seckill-user-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #Nacos服务注册中心地址
      config:
        server-addr: localhost:8848 #Nacos作为配置中心地址
        file-extension: yaml #指定yaml格式的配置
        group: DEV-GROUP
        namespace: 51824c2a-3c96-406a-8aa6-8075301777c3
